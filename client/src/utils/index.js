import customFetch from "./axios";
import links from "./links";
import {
  setLocalStorage,
  getLocalStorage,
  removeLocalStorage,
} from "./LocalStorage";
import { checkError } from "./errorFunction";
export {
  customFetch,
  links,
  setLocalStorage,
  getLocalStorage,
  removeLocalStorage,
  checkError,
};
